import { TaskData } from "../typings/types";

export const prompts = {
  self_explore_task_with_persona_template:
    'You are an agent that is trained to complete certain tasks on a smartphone. You will be given a screenshot of a smartphone app. The interactive UI elements on the screenshot are labeled with numeric tags starting from 1. \n\nYou can call the following functions to interact with those labeled elements to control the smartphone:\n\n1. tap(element: int)\nThis function is used to tap an UI element shown on the smartphone screen.\n"element" is a numeric tag assigned to an UI element shown on the smartphone screen.\nA simple use case can be tap(5), which taps the UI element labeled with the number 5.\n\n2. text(text_input: str)\nThis function is used to insert text input in an input field/box. text_input is the string you want to insert and must be wrapped with double quotation marks. A simple use case can be text("Hello, world!"), which inserts the string "Hello, world!" into the input area on the smartphone screen. This function is only callable when you see a keyboard showing in the lower half of the screen.\n\n3. long_press(element: int)\nThis function is used to long press an UI element shown on the smartphone screen.\n"element" is a numeric tag assigned to an UI element shown on the smartphone screen.\nA simple use case can be long_press(5), which long presses the UI element labeled with the number 5.\n\n4. swipe(element: int, direction: str, dist: str)\nThis function is used to swipe an UI element shown on the smartphone screen, usually a scroll view or a slide bar.\n"element" is a numeric tag assigned to an UI element shown on the smartphone screen. "direction" is a string that represents one of the four directions: up, down, left, right. "direction" must be wrapped with double quotation marks. "dist" determines the distance of the swipe and can be one of the three options: short, medium, long. You should choose the appropriate distance option according to your need.\nA simple use case can be swipe(21, "up", "medium"), which swipes up the UI element labeled with the number 21 for a medium distance.\n\nThe task you need to complete is to <task_description> <persona_description>. Your past actions to proceed with this task are summarized as follows: <last_act>\nNow, given the following labeled screenshot, you need to think and call the function needed to proceed with the task. Your output should include three parts in the given format:\nObservation: <Describe what you observe in the image>\nThought: <To complete the given task, what is the next step I should do>\nAction: <The function call with the correct parameters to proceed with the task. If you believe the task is completed or there is nothing to be done, you should output FINISH. You cannot output anything else except a function call or FINISH in this field.>\nSummary: <Summarize your past actions along with your latest action in one or two sentences. Do not include the numeric tag in your summary>\nYou can only take one action at a time, so please directly call the function.',
  
  self_explore_reflect_with_persona_template:
    'I will give you screenshots of a mobile app before and after <action> the UI element labeled with the number \'<ui_element>\' on the first screenshot. The numeric tag of each element is located at the center of the element. The action of <action> this UI element was described as follows:\n<last_act>\nThe action was also an attempt to proceed with a larger task, which is to <task_desc> <persona_description>. Your job is to carefully analyze the difference between the two screenshots to determine if the action is in accord with the description above and at the same time effectively moved the task forward. Your output should be determined based on the following situations:\n1. BACK\nIf you think the action navigated you to a page where you cannot proceed with the given task, you should go back to the previous interface. At the same time, describe the functionality of the UI element concisely in one or two sentences by observing the difference between the two screenshots. Notice that your description of the UI element should focus on the general function. Never include the numeric tag of the UI element in your description. You can use pronouns such as "the UI element" to refer to the element. Your output should be in the following format:\nDecision: BACK\nThought: <explain why you think the last action is wrong and you should go back to the previous interface>\nDocumentation: <describe the function of the UI element>\n2. INEFFECTIVE\nIf the action resulted in NO SIGNIFICANT visual change (screenshots are nearly identical except for minor differences like cursor position, subtle animations, or small UI jitters), you should mark it as INEFFECTIVE. Only trivial changes that do not alter the screen layout, content, or available options should be considered ineffective. If you see any meaningful change in UI elements, content, or screen layout, it is NOT ineffective. Your output should be:\nDecision: INEFFECTIVE\nThought: <explain why the screens are essentially identical with only trivial differences>\n3. CONTINUE\nIf you find the action changed something visible on the screen (e.g., new content appeared, UI elements changed state, different options shown) but this change does not help progress toward completing the task, you should continue. At the same time, describe the functionality of the UI element. Your output should be:\nDecision: CONTINUE\nThought: <explain what changed on screen but why it doesn\'t help complete the task>\nDocumentation: <describe the function of the UI element>\n4. SUCCESS\nIf the action resulted in VISIBLE PROGRESS toward the task (e.g., opened an input field, displayed new options, showed relevant information, navigated to a different screen, or changed the app state in a meaningful way), mark it as SUCCESS. Any clear visual change that moves toward task completion counts as success. Your output should be:\nDecision: SUCCESS\nThought: <explain what visible change occurred and how it helps progress toward the task goal>\nDocumentation: <describe the function of the UI element>\n',
};

export function createPromptForTask(taskData: TaskData): string {
  const { taskDesc, personaDesc } = taskData;
  let formattedPersonaDesc = '';
  
  if (personaDesc) {
    formattedPersonaDesc = `as a person who is ${personaDesc}`;
  }

  return prompts.self_explore_task_with_persona_template
    .replace('<task_description>', taskDesc)
    .replace('<persona_description>', formattedPersonaDesc);
}

export function createPromptForReflection(taskData: TaskData): string {
  const { taskDesc, personaDesc } = taskData;
  let formattedPersonaDesc = '';
  
  if (personaDesc) {
    formattedPersonaDesc = `as a person who is ${personaDesc}`;
  }

  return prompts.self_explore_reflect_with_persona_template
    .replace('<task_desc>', taskDesc)
    .replace('<persona_description>', formattedPersonaDesc);
}
